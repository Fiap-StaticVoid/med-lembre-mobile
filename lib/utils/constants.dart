import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Colors.green;
  static const accentColor = Colors.greenAccent;
}

class AppStyles {
  static const headlineStyle = TextStyle(
    fontSize: 24,
    fontWeight: FontWeight.bold,
  );
  static const bodyTextStyle = TextStyle(
    fontSize: 16,
  );
}

class AppStrings {
  static const appName = 'Health App';
  // Adicione mais strings que são usadas frequentemente em todo o aplicativo
}
